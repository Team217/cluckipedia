# Java Tutorial | Quiz 4 Connecting Hardware

## What's the difference between motors and motor controllers?

- **A** - There is no such thing as a motor controller. It is just a common misconception in programming.
- **B** - Motors are given code to move, and the motor controller validates that code to make sure the motor is moving correctly.
- **C** - Motors tell the code data about its movement. Motor controllers make sure that data get's to the code correctly.
- **D** - Motor controllers control the motors; the motors just follow what the controller is telling it what to do.

<br>
<details>
  <summary>Click to reveal answer.</summary>
  D Motor controllers control the motors; the motors just follow what the controller is telling it what to do.
</details>

## What's the difference between limit switches and banner sensors?

- **A** - Limit switches use colors to determine what data to output.
- **B** - One uses a physical button, while the other uses a beam.
- **C** - They are the same thing, both mechanically and technically.
- **D** - None of the above.

<br>
<details>
  <summary>Click to reveal answer.</summary>
  B One uses a physical button, while the other uses a beam.
</details>

## What does an encoder do?

- **A** - Sends data to the motors from the code.
- **B** - It validates that the motor is doing what the code says.
- **C** - Sends data from the motors back to the code.
- **D** - All of the above.

<br>
<details>
  <summary>Click to reveal answer.</summary>
  C Sends data from the motors back to the code.
</details>

## How do we tune the hardware we talked about?

- **A** - Using GABC values.
- **B** - Using CDEF values.
- **C** - Using PIDF values.
- **D** - Using LRCS values.

<br>
<details>
  <summary>Click to reveal answer.</summary>
  C Using PIDF values.
</details>

<br>
Made with ♡ by Jacob