[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Cluckipedia"
authors = [{name = "ThunderChickens Programming Department", email = "thunderchickens@gmx.com"}]
dynamic = ["version", "description"]
